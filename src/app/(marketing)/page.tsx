
// This file is kept to ensure the (marketing) route group exists,
// but its content is now served by src/app/page.tsx for the root path.
// You can add other marketing pages here, e.g., /about, /pricing, etc.
// by creating src/app/(marketing)/about/page.tsx.

export default function MarketingSubPagePlaceholder() {
  return null; // Does not render anything itself for the path /
}

    
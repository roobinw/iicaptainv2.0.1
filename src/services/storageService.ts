
// import { ref, uploadBytes, getDownloadURL } from 'firebase/storage';
// import { storage } from '@/lib/firebase'; // Ensure storage is exported from firebase.ts
// import type { FirebaseError } from 'firebase/app';

// Avatar upload functionality is removed as per user request to use URL input.
// The storageService.ts file is kept in case other storage operations are needed in the future.
// If no other storage operations are planned, this file could be deleted.

/**
 * (REMOVED) Uploads an avatar image to Firebase Storage and returns the download URL.
 * This function is no longer used as avatars are handled via direct URL input.
 */
// export const uploadAvatar = async (userId: string, file: File): Promise<string> => {
//   // ... implementation removed ...
// };
